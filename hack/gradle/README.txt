This directory is a hack to enable IDE support for gradle build files.

The gradle build files are symlinked here so that they can be worked on with code completion, as their actual location is not gradle enabled.
