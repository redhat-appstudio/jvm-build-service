apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: hacbs-jvm-operator
resources:
  - system-config.yaml

patchesStrategicMerge:
  #note that when updating the ref here you must also update system-config.yaml
  - https://raw.githubusercontent.com/redhat-appstudio/jvm-build-service-builder-images/b21afff3412984bb7e06e9a03c7097db201d3f55/image-config.yaml
