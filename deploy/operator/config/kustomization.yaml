apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- system-config.yaml

labels:
- includeSelectors: true
  pairs:
    app: hacbs-jvm-operator

#note that when updating the ref here you must also update system-config.yaml
patches:
- path: https://raw.githubusercontent.com/redhat-appstudio/jvm-build-service-builder-images/c2e2b96a105f45bbb613f93cbfd5d7fddd96154a/image-config.yaml
